// use chrono::{DateTime, MappedLocalTime, TimeZone, Utc};


// pub fn datetime_to_timestamp(date: chrono::DateTime<Utc>) -> u64 {
//     date.timestamp() as u64 // Convert to u64 to ensure no negative values
// }

// pub fn timestamp_to_datetime(timestamp: u64) -> MappedLocalTime<DateTime<impl TimeZone>>{
//     Utc.timestamp_opt(timestamp as i64, 0) // Convert back to DateTime
// }
